export default class Permiso{
    constructor(cedula, fecha, descripcion, costoSalarial){
        (this.cedula = cedula),
        (this.fecha = fecha),
        (this.descripcion = descripcion),
        (this.costoSalarial = costoSalarial)
    }
}
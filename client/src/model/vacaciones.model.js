export default class Vacaciones{
    constructor(cedula, fecha_salida, fecha_entrada){
        (this.cedula = cedula),
        (this.fecha_salida = fecha_salida),
        (this.fecha_entrada = fecha_entrada)
    }
}
export default class HorasExtra{
    constructor(cedula, cantidadHoras, motivo, fecha){
        (this.cedula = cedula),
        (this.cantidadHoras = cantidadHoras),
        (this.motivo = motivo),
        (this.fecha = fecha)
    }
}
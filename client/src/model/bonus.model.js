export default class Bonus{
    constructor(cedula, motivo, cantidad, fecha){
        (this.cedula = cedula),
        (this.motivo = motivo),
        (this.cantidad = cantidad),
        (this.fecha = fecha)
    }
}
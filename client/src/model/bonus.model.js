export default class Bono{
    constructor(cedula, motivo, cantidad, fecha){
        (this.cedula = cedula),
        (this.motivo = motivo),
        (this.cantidad = cantidad),
        (this.fecha = fecha)
    }
}